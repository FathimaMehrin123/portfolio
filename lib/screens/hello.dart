import 'package:flutter/material.dart';

class bfhhd extends StatelessWidget {
  const bfhhd({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('dhsh'),
    );
  }
}